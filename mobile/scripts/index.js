$(document).ready(function(){
    
    // Load cookieSettings
    // (automatic loading in cookies.js DOM)

    // Load appearance
    window.loadAppearance();

    // Load the team grid
    window.grid_reset();

    // Load team results
    window.load_results();
});